Create a new rule of validations in database validations
---
parameters:
  - in: body
    name: regla
    type: string
    required: true
  - in: body
    name: cantidad
    type: string
    required: false
responses:
  201:
    description: A single rule item
    schema:
      data: object

