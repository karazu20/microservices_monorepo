Create a new user app
---
parameters:
  - in: body
    required: true
    name: username
    type: string
  - in: body
    required: true
    name: email_user
    type: string
responses:
  201:
    description: Create new user app
    schema:
      data: object

